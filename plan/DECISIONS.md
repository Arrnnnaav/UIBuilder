# Decisions (ADR log)

| # | Date | Decision | Why |
|---|---|---|---|
| D1 | 2026-09-24 | 7 agents + Orchestrator (8 roles), 3 gates, files as the contract | 18 agents meant too much handoff overhead |
| D2 | 2026-09-24 | No Figma. The design source is `DESIGN.md` + `tokens.css` + `/styleguide` | Figma is paid; the user wants minimum cost |
| D3 | 2026-09-24 | Portfolio on Vercel Hobby; commercial clients on Cloudflare Workers in a client-owned account | Hobby forbids commercial use; Cloudflare is free and allows it |
| D4 | 2026-09-24 | No auth on marketing sites; the SaaS recipe uses Clerk (Better Auth as the $0 fallback) | Nothing to log into; less cost and attack surface |
| D5 | 2026-09-24 | Keystatic in GitHub mode for blog and case studies | Free, git-based, and the admin uses GitHub OAuth |
| D6 | 2026-09-25 | MiniMax H3, Higgsfield, HeyGen and AI-citation tracking are registered in `brain/tools.json` but disabled | H3 open weights are free but 768p and need a GPU; its API is ~$0.08–0.13/s. Higgsfield is paid ($19/mo+, $3 trial). Enable when there's budget |
| D7 | 2026-09-25 | New **growth** agent owns SEO/AEO/GEO during the build; BusinessOS owns it afterwards; both use the same rules | The user wants SEO/AEO/GEO built in from day one and maintained after launch |
| D8 | 2026-09-25 | BusinessOS spec amended with **R7 Git-site connector** (`business-os/docs/superpowers/specs/2026-09-25-git-site-connector-design.md`): owner approval opens a PR, a separate owner approval merges, and only manifest paths can be edited | Fixes the conflict with D4 and "Explicitly not building" |
| D9 | 2026-09-25 | Rules live in `AGENTS.md`; `CLAUDE.md` only imports it | User preference; works across tools |
| D10 | 2026-09-25 | Adopted from the second plan as schema only: provenance, pattern × project-type scores, design lineage, a deterministic tool router. Deferred: a custom agent runtime, pgvector, a knowledge graph | Keeps V1 cheap and quick while staying ready to migrate |
| D11 | 2026-09-25 | BusinessOS M5 deviations accepted for now: (a) rollback (`site.git.pr.rollback`) is allowed while the kill switch is on, since it only closes or reverts BusinessOS-owned PRs; (b) the post-publish verify miss keeps the experiment PUBLISHED and `/verify` re-checks later; (c) publish is pinned to the approved head commit; (d) rejecting a publish approval doesn't yet move the experiment out of AWAITING_PUBLISH | Deploys lag the merge. **Needs user confirmation on (a).** |
| D12 | 2026-09-25 | Static CSP with `'unsafe-inline'` scripts (no nonce) on marketing sites | A nonce forces dynamic rendering, which costs perf and free-tier compute. The allowlist is otherwise tight and includes frame-ancestors none |
| D13 | 2026-09-25 | Local perf gate = `scripts/perf.mjs` (Playwright + Lighthouse); lhci only in Linux CI | chrome-launcher EPERM on Windows |
| D14 | 2026-09-25 | ABizCreator V1 is **frontend-only**: a static site with no server actions and no contact backend. Contact goes through tel, WhatsApp and mailto links. Blog and case studies are static content files in git, so BusinessOS can edit them by PR. | User direction: frontend first, backend later |
| D15 | 2026-09-25 | ABizCreator moves from WordPress to the new git-based site. The existing WordPress site stays as **staging** and as the BusinessOS WordPress connection during the migration. The git-site connector takes over once the new site is live. | User direction. Keeps the existing BusinessOS pilot path working meanwhile |
| D16 | 2026-09-25 | Portfolio content comes from the résumé (`C:/Users/user/Downloads/ArnavResume.pdf`) and GitHub `Arrnnnaav`. Only public repos are shown unless the user OKs private ones. | Private repos may hold client or employer code |
| D17 | 2026-09-25 | Portfolio positioning: "fast, auditable, honest". Résumé-only numbers stay hidden until the owner commits sources (`pending-source` rows) | Owner, at G1 |
| D18 | 2026-09-25 | ABizCreator leads with Print; its G1 is on hold until the client answers | Owner, at G1 |
| D19 | 2026-09-25 | ABizCreator gets a quote form through **Formboost** (free tier, 500 submissions/month), still with no backend of our own. It is a static HTML POST with a honeypot, WhatsApp stays the primary CTA, and the form is secondary. It will be built in S4, after the provider's limits, spam protection and data handling are checked | Owner said "ok"; this fits D14 and the free-tiers-first rule |
| D20 | 2026-09-26 | Publish UIBuilder with current `projects/*` source as ordinary folders in the root GitHub repository. Preserve the three uncommitted nested Git directories in ignored `.git-metadata-backup/`. Keep deployment repository strategy open for each site; `new-project.mjs` still initializes standalone Git and needs a conscious choice before the next project is created. | The owner requested a complete push to `Arrnnnaav/UIBuilder`; empty nested Git repositories would otherwise stage as gitlinks without source. |
