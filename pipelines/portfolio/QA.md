# QA additions: portfolio
- Every `/work/[slug]` page loads with one h1, and its images have alt text.
- On touch devices, the work-index hover preview falls back to an inline expand.
- Case-study images use `next/image` with `sizes`, and mobile LCP stays under 2.5s.
